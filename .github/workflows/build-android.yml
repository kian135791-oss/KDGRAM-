name: KDGRAM Android Build

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "KDGRAM_GITHUB_READY.zip"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Setup Android
        uses: android-actions/setup-android@v3

      - name: Extract KDGRAM
        run: |
          mkdir -p kdgram
          unzip -o KDGRAM_GITHUB_READY.zip -d kdgram
          rm -rf kdgram/.github

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.15.0

      - name: Install dependencies
        working-directory: kdgram
        run: pnpm install --no-frozen-lockfile

      - name: Generate Android project
        working-directory: kdgram/apps/mobile
        run: npx expo prebuild --platform android --no-install

      - name: Build APK
        working-directory: kdgram/apps/mobile/android
        run: chmod +x gradlew && ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: KDGRAM-Android-APK
          path: kdgram/apps/mobile/android/app/build/outputs/apk/debug/app-debug.apk
